# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json

# Claude Code project instructions (kept local only, not published)
CLAUDE.md

# Claude Code skills (kept local only, not published)
.claude/skills/

# Claude Code agent worktrees (transient checkouts, live inside the repo)
.claude/worktrees/

# Agent-skill config and agent-facing dev docs (kept local only, not published)
docs/
CONTEXT.md
CONTEXT-MAP.md

# Obsidian local view state (churns from normal app usage, not real config)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/graph.json

# obsidian-git plugin's local auth/state (device-specific, not shared)
.obsidian/plugins/obsidian-git/data.json

# Per-vault backups written by the framework updater (99 - Meta/04 - Tooling)
99 - Meta/05 - Backups/

# Movie/Series design record and API survey (kept local only, not published).
# Pushed temporarily so the #57 agent run could read them from a fresh clone.
research/movie-series-capture-design.md
research/movie-metadata-apis.md
